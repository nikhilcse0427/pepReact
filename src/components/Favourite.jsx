/* eslint-disable react/prop-types */

// Destructure the "vehicle" prop
const FavoriteVehicle = ({ vehicle }) => {
  return <p>My favorite vehicle is: {vehicle}</p>;
};

export default FavoriteVehicle;
